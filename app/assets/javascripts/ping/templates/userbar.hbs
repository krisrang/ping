<div class="userbar">
  {{#if currentUser.isLoggedIn}}
    <div class="userbar-details" data-toggle="dropdown">
      <i class="fa fa-chevron-up up"></i>      
      {{boundAvatar currentUser imageSize="medium"}}
      <span>{{currentUser.username}}</span>
      {{render "connectbit"}}
    </div>
    
    <ul class="userbar-settings dropdown-menu" role="menu" aria-labelledby="dLabel">
      <li>{{#link-to "preferences"}}<i class="fa fa-cog"></i> {{i18n userbar.preferences}}{{/link-to}}</li>
      <li><a {{action "logout"}}><i class="fa fa-sign-out"></i> {{i18n userbar.logout}}</a></li>
    </ul>
  {{/if}}
</div>